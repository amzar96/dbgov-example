apiVersion: dbgov/v1
kind: AccessPolicy
metadata:
  name: analyst-readonly
spec:
  principal:
    name: analyst_user
    type: user
  grants:
    - level: table
      schema: public
      tables:
        - transactions
        - accounts
      privileges:
        - SELECT
