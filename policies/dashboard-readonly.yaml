apiVersion: dbgov/v1
kind: AccessPolicy
metadata:
  name: dashboard-readonly
spec:
  principal:
    name: dashboard_user
    type: user
  grants:
    - level: table
      schema: public
      tables:
        - users
        - orders
        - customers
      privileges:
        - SELECT
        - UPDATE
